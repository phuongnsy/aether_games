// The content layer's translation unit. `Level`, `MakeLevel` and `WallQuery`
// are defined in the header they were extracted from; this exists so the
// layer is a LIBRARY like every other one rather than a header nobody links.
#include "infiltration/content/level.hpp"

namespace infiltration::content {

// Nothing out of line yet. Splitting the definitions out is a change to the
// code, and i2 is a change to the LAYOUT — one at a time.

}  // namespace infiltration::content
